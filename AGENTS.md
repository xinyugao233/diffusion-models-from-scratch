# Project Guidance

- Follow the workspace research rules and `PROJECT_SPEC.md`.
- Derive mathematical behavior before implementing it.
- Use PyTorch and explicit random seeds.
- Keep model-space CIFAR-10 images in `[-1, 1]` and invert normalization before
  visualization.
- Do not launch training or expensive computation without explicit authorization.
- Never overwrite completed experimental tries.
